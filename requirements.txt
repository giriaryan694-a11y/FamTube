# FamTube - Family Video Sharing Platform
# Python Dependencies

# Core Web Framework
Flask>=2.3.0

# Security & Rate Limiting
Flask-Talisman>=1.0.0
Flask-Limiter>=3.5.0
Flask-WTF>=1.1.0

# Real-time Features (Live Streaming, Notifications)
Flask-SocketIO>=5.3.0
python-socketio>=5.8.0

# Werkzeug (included with Flask, but pinned for security)
Werkzeug>=2.3.0

# Jinja2 (included with Flask, used for DictLoader)
Jinja2>=3.1.0
